import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
      alignItems: 'center',
      flex: 1,
    },
    title: {
      fontSize: 24,
      fontFamily: 'san-francisco',
      color: '#ffffff'
    },
  });